<h1> Update User </h1>

<%= form_for(:user, :url => {:action => 'update', :id => @user.id}) do |f| %>

  <%= render(:partial => "form", :locals => {:f => f}) %>

  <%= submit_tag("Update User") %>

<% end %>